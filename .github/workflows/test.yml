# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/cli/src/commands/setup/run-setup.ts
# Tool:    holocron setup
# Changes: edit source in theholocron/holocron
name: Test

on: # yamllint disable-line rule:truthy
  push:
    branches: [main, alpha]
  pull_request:

concurrency:
  group: test-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write
  statuses: write

jobs:
  test:
    name: Test
    uses: theholocron/.github/.github/workflows/test.yml@main
    with:
      run-unit: false
      run-storybook: true
      run-interaction: true
      run-user-flow: true
      run-chromatic: true
      chromatic-projects: '[{"tokenName":"default","workingDir":".","buildScript":"build:storybook:chromatic"}]'
    secrets: inherit
