---

extends: default

rules:
  comments:
    min-spaces-from-content: 1

  comments-indentation: disable

  document-start:
    ignore: |
      .github/*.yml

  indentation:
    spaces: 2

  line-length: disable
